```
Initialize Pi Camera and load trained model
Start capturing frames continuously

While True:
    Capture a frame from the camera
    Process the frame using the TensorFlow model
    if gyroscope detects 3 laps have been made:
        stop the code

End
```
